{
  "name": "movie-booking",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "amplify"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "sandbox": "node scripts/with-env.mjs npx ampx sandbox",
    "sandbox:delete": "node scripts/with-env.mjs npx ampx sandbox delete"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
